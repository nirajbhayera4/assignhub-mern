import { useEffect } from 'react';

export const useAnimations = () => {
  useEffect(() => {
    // All GSAP animations have been removed
    // This hook is now empty to maintain compatibility
  }, []);
};